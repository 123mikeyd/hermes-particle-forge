# Assets

- `demo.mp4` — short capture of Particle Forge V2 in motion.

The older `ParticleForgeV1.gif` capture was intentionally not committed because it is ~180 MB. If a GIF is needed later, generate a compressed/web-optimized version from the video before adding it to the repository.
