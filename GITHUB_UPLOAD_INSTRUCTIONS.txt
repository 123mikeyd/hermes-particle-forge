GitHub upload was not completed because gh is not authenticated on this machine.

To upload:

1. Open a terminal:
   cd /mnt/c/Users/Mike/Desktop/ParticleForgeV2CoolPass

2. Authenticate GitHub CLI:
   gh auth login

3. Create and push a private repo:
   gh repo create particle-forge-v2-cool-pass --private --source . --push

If you want public instead, replace --private with --public.
